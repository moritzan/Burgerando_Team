package Klassen.UnitTests;

import static org.junit.Assert.*;

import java.util.ArrayList;

import org.junit.Test;

import Klassen.DatenManager;
import Klassen.KontaktInformationen;
import Klassen.BurgerBestandteil;

public class DatenManagerTestBurger {

	@Test
	public void test() {
		
		fail("Not implemted");
		/*
		DatenManager dm = new DatenManager(true);
		
		ArrayList<BurgerBestandteil> data = new ArrayList<BurgerBestandteil>();
		data.add(new BurgerBestandteil("Name", )));
		//speichere Daten
		dm.saveBurgerBestanteile(data);
		
		//Wandle Daten in Array um, damit vergleich funktioniert
		BurgerBestandteil[] expected = new BurgerBestandteil[data.size()];
		expected = (BurgerBestandteil[]) data.toArray();
		
		//Lade Restaurant
		BurgerBestandteil[] actual= dm.loadBurgerbestandteile();
		
		
		assertArrayEquals(expected, actual);
		*/
	}

}
